


# autocomplete functions
fpath=(~/.dotfiles/module/zsh-completions/src/ $fpath)

if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
 source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
fi


# zpreztorc's history file is ~/.zhistory not ~/.zsh_history
# you can you `history 0` to display all command
# change the ... = '../../'


# added by travis gem
[ -f /home/yantze/.travis/travis.sh ] && source /home/yantze/.travis/travis.sh

# zprezto history module
# Press ^U (the Control and U keys simultaneously) to abort the search.
# bind UP and DOWN arrow keys
# bindkey "$terminfo[kcuu1]" history-substring-search-up
# bindkey "$terminfo[kcud1]" history-substring-search-down


