https://www.python.org/downloads/
http://git-scm.com/download/win
添加字体YaHei.Consolas.1.12.Revise
:PluginInstall


安装ycm的步骤
如果不使用c/c++的语义提示，就直接执行ycm项目目录的./install.sh
如果要使用语义提示，需要安装yum isntall clang cmake28
可以用locate libclang.so查找libclang.so的位置，比如结果是/usr/lib64/llvm/libclang.so
切换到ycm的项目目录，然后执行下面的语句
cmake28 -G "Unix Makefiles" . -DPATH_TO_LLVM_ROOT=/usr/lib64/llvm ~/.vim/bundle/YouCompleteMe/third_party/ycmd/cpp
make ycm_support_libs
就可以安装成功ycm了


